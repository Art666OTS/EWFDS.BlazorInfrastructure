# Api (placeholder)

Reserved for genuinely API-only infrastructure. Currently empty: all server-host code (HttpContext, middleware, endpoints) that is shared by both Blazor and API hosts lives under 'Common'. Add code here only when it is needed exclusively by a pure Web API host.
